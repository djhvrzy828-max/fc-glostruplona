'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase'
export default function Page(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const s=createClient();const {error}=await s.auth.signInWithPassword({email:String(fd.get('email')),password:String(fd.get('password'))});if(error){setError('Forkert email eller adgangskode');return}router.push('/admin');router.refresh()}return <div className="mx-auto max-w-md card p-8"><h1 className="text-3xl font-black">FC GLOSTRUPLONA ADMIN</h1><p className="mt-2 text-sm text-neutral-400">Kun godkendte administratorer.</p><form onSubmit={submit} className="mt-6 grid gap-4"><input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="password" type="password" placeholder="Password" required/>{error&&<div className="text-sm text-red-400">{error}</div>}<button className="btn">LOG IND</button></form></div>}
